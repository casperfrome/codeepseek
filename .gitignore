# Build output
[Bb]in/
[Oo]bj/
publish/
[Pp]ublish/
*.dll
*.exe
*.pdb

# Visual Studio / Rider
.vs/
.idea/
*.user
*.suo
*.userprefs

# NuGet
*.nupkg
packages/

# Go build output
/control/mbcontrol
/control/mbcontrol.exe

# OS junk
Thumbs.db
.DS_Store

# Secrets / runtime files that live in the moon-bridge backend dir. Never commit
# them even if a copy ends up here: mb_config.json holds real API keys.
mb_config.json
config.yml
config.yml.bak
bridge*.log

# Local app data captured during runtime (WebView2 cache, config.json) lives in
# %LocalAppData%\MoonBridgeSwitcher and is never part of the repo.
